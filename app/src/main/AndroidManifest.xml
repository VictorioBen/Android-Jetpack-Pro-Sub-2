<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.submission.victorio_jetpackpro">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Victorio_jetpackpro"
        tools:ignore="AllowBackup">
        <activity android:name="ui.tv.detail.DetailTvActivity"
            android:theme="@style/AppTheme.TransparentBar" />
        <activity
            android:name="ui.movies.detail.DetailMovieActivity"
            android:theme="@style/AppTheme.TransparentBar" />
        <activity
            android:name="ui.home.HomeActivity"
            android:theme="@style/Theme.Victorio_jetpackpro.NoActionBar"
            />
        <activity
            android:name="ui.splashscreen.SplashScreenActivity"
            android:theme="@style/Theme.MaterialComponents.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>